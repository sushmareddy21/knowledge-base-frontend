# NOTE: We use VITE_ prefix because we are using Vite, not Create-React-App.
# Replace the URL below with your ACTUAL Railway URL from Step 2.5
VITE_API_URL=https://documentserviceapplication-production.up.railway.app/api